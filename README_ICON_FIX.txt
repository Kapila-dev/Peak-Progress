Peak Progress v26 icon fix.

Upload/replace all files in the GitHub repo root, including:
- index.html
- manifest.webmanifest
- sw.js
- icon-192.png
- icon-512.png
- .nojekyll

Then wait for GitHub Pages to redeploy. Delete the old home-screen shortcut, open the live URL in Chrome, refresh once, and install again.
